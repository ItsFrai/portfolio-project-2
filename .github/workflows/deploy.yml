name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./app
          push: true
          tags: frai333/portfolio-app-v2:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Deploy to EC2 Instance 1
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST_1 }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          script: |
            sudo docker pull frai333/portfolio-app-v2:latest
            sudo docker stop portfolio-app || true
            sudo docker rm portfolio-app || true
            sudo docker run -d -p 80:80 --restart always --name portfolio-app frai333/portfolio-app-v2:latest

      - name: Deploy to EC2 Instance 2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST_2 }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          script: |
            sudo docker pull frai333/portfolio-app-v2:latest
            sudo docker stop portfolio-app || true
            sudo docker rm portfolio-app || true
            sudo docker run -d -p 80:80 --restart always --name portfolio-app frai333/portfolio-app-v2:latest
